const { aliases } = require("../aliasList.js");
exports.run = (client, message, args) => {
  if (!args.length) {
    message.channel.send("Alias add to add an alias, and Alias remove to remove one! Alias list for a list!");
  }
  if (args[0] === 'add') {
    
  }
}
